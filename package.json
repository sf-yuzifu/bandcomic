{
  "name": "bandcomic",
  "version": "1.7",
  "description": "",
  "engines": {
    "node": ">=8.10"
  },
  "scripts": {
    "start": "aiot start --watch --enable-custom-component",
    "build": "aiot build --enable-custom-component",
    "release": "aiot release --enable-custom-component  --enable-custom-component --enable-protobuf --enable-jsc",
    "lint": "eslint --format codeframe --fix --ext .ux,.js src/"
  },
  "devDependencies": {
    "@aiot-toolkit/jsc": "^1.0.3",
    "aiot-toolkit": "^2.0.4"
  }
}